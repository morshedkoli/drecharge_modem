<?xml version="1.0" encoding="utf-8"?>
<!--
  Event types and flags here must match what onServiceConnected() sets at runtime.
  The XML declaration is what Android uses as the authoritative source on many OEMs
  (MIUI, Samsung, ColorOS) — mismatches can cause the service to be silently downgraded.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewTextChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:description="@string/ussd_service_description"
    android:notificationTimeout="1000" />
